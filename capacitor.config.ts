import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fh_stp.ionic_todo_app',
  appName: 'ionic_todo_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
